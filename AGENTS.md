# AGENTS.md

- For local UI checks, run Vite on `127.0.0.1:5173` and verify with Playwright if the in-app browser blocks the localhost page.
- Use a simple PNG fixture for image upload tests; SVG favicon files may fail the app's decode path in headless checks.
