export type LoadedImage = {
  fileName: string
  imageData: ImageData
}

export type HistoryEntry = {
  id: number
  label: string
  fileName: string
  imageData: ImageData
}

export type ExpandedImage = {
  label: string
  fileName: string
  imageData: ImageData
}
